<label for="{{ fieldName }}" class="form-input-field__name">{{ fieldLabel }}</label>
<input id="{{ fieldName }}" name="{{ fieldName }}" class="form-input-field__input" type="{{ inputType }}" placeholder="{{ placeholder }}" value="{{#if value}}{{value}}{{/if}}" />
<p class="form-input-field__error-text">{{ errorText }}</p>
