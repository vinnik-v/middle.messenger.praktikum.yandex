<div class="form-input-field">
    <div class="form-input-field__name">{{ fieldName }}</div>
    <input class="form-input-field__input" type="{{ inputType }}" placeholder="{{ inputPlaceholder }}" value="{{#if value}}{{value}}{{/if}}">
    <div class="form-input-field__error-text">{{ errorText }}</div>
</div>