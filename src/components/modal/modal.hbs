<div class="modal display-none" id="{{id}}">
    <div class="{{#if contentClassName}}{{contentClassName}}{{/if}}">
        {{#if modalTitle}}
        <h1 class="modal__title">
            {{ modalTitle }}
        </h1>
        {{/if}}
        {{> @partial-block }}
    </div>
</div>